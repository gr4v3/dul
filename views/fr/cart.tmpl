<header>
    <h1>Vos tee-shirts</h1>
    <i class="fa-solid fa-circle-xmark"></i>
</header>
<main>
    <table>
        <tbody>
        {{#checkout}}
            <tr>
                <td>Taille {{size}}</td>
                <td>x{{amount}}</td>
                <td>{{total | price}}</td>
            </tr>
        {{/checkout}}
        </tbody>
        <tfoot>
            <tr>
                <td colspan="3">total: {{total | price}}</td>
            </tr>
        </tfoot>
    </table>
</main>
<footer>
    <button type="button" class="btn btn-danger">Annuler</button>
    <button type="button" class="btn btn-success">Vérifier</button>
</footer>